# Local desk state — regenerated by the fetchers, never shared.
data/desk.db
data/desk.db-*
data/raw/*
data/cache/*

# Pasted content from gated sources (screenshots, X posts, Pikkit, Action PRO).
manual/*

!.gitkeep
